import { useState } from "react"
import { LogOut } from "lucide-react"
import { Button } from "../ui/Button"
import { DialogFrame } from "../ui/DialogFrame"
export function SignOutDialog({ onClose, onConfirm }: { onClose(): void; onConfirm(): Promise<void> }) { const [busy, setBusy] = useState(false); const [error, setError] = useState<string | null>(null); async function confirm() { setBusy(true); setError(null); try { await onConfirm(); onClose() } catch (cause) { setError(cause instanceof Error ? cause.message : String(cause)); setBusy(false) } } return <DialogFrame presentation="modal" className="logout-dialog" hideFooter title="Sign out?" busy={busy} error={error} onClose={onClose} onSubmit={event => event.preventDefault()}><div className="logout-confirmation"><i><LogOut /></i><div><strong>Sign out of GetGo Tools?</strong><span>Your local quiz files will remain unchanged. The Firebase session will be removed from this device.</span></div></div><div className="logout-actions"><Button disabled={busy} onClick={onClose}>Cancel</Button><Button icon={<LogOut />} loading={busy} variant="solid" color="danger" onClick={() => void confirm()}>Sign out</Button></div></DialogFrame> }
