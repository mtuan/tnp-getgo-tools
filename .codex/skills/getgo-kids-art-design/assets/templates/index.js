const page = document.querySelector('#app');

page.innerHTML = `<!-- Replace with exact semantic content and controls. -->`;

KidsDesign.bindTheme({ toggle: document.querySelector('.theme-toggle') });
